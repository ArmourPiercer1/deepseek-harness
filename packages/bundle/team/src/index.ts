/**
 * Agent team bundle entry.
 *
 * This module re-exports nothing — the bundle is a pure `cordis.patch.yml`
 * composition. The entry exists so the package has a valid `main` field.
 *
 * @module @deepseek-ai/dsh-bundle-team
 */
